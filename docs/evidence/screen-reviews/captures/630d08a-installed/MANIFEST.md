# Installed-app captures — 630d08a preview (generated note), 2026-09-02 evening

Device: this Mac, packaged `Yawn Preview.app` built from 630d08a, runtime staged
with `build-alpha-external` and signed with the Developer ID (nested Mach-Os),
real storage with the same four meetings, window at its 1080x900 default,
captured by live window bounds, read back from disk before inclusion. Light
appearance via the preview bundle's own defaults domain, removed after. First
run not staged. The note in frame 02 was generated on this Mac during the
capture run by the packaged gemma-3-12b model (25 s for nine turns) and
assembled by the worker; it is the first generated note any packaged build
has produced (D-NOTE-STAGE, four gates).

| File | State | Notes |
|---|---|---|
| 01-dark-transcript-only | The nine-turn transcript-only meeting before generation: audio released, no note, Generate note offered | |
| 02-dark-note | The same meeting after generation: Note, an Overview with two cited claims, the row carries the note's first line | |
| 03-dark-summary-failed | The Sep 1 meeting whose generation failed earlier (zero-turn transcript, rejected honestly) | R23 evidence: what the summary-failed state shows |
| 04-light-summary-failed | Same as 03 in light; the nine-turn row shows its note line | |
| 05-light-rename-sheet | The rename sheet over the nine-turn meeting with its note, light | The light note is visible only blurred behind the sheet |

Not captured: the generating state (accessibility could not read the window
while the generation ran, so no frame was taken; whether the window was
merely unreadable to System Events or unresponsive to the person is
unverified and filed for a check); the light note frame directly (two
relaunches opened the first row instead, the row click resolving late);
the note inspector; Settings, first run, and the interrupted state (unchanged
since the 88da6b6 set). The Record control is disabled in every frame because
system-audio access has not been granted to the preview bundle.
